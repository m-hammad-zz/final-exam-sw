<h1>Listing sales</h1>
<!--
= form_tag movies_path, :id => "ratings_form",  :method => :get do
Include:
- @all_ratings.each do |rating|
= rating
= check_box_tag "ratings[#{rating}]", @ratings[rating], @ratings[rating] ? true : false, :id => "ratings_#{rating}"
= submit_tag 'Refresh', :id=>"ratings_submit"
-->

<%= form_tag sales_path, id: "cities_form", method: :get do %>
    <% @cities.each do |city| %>
        <%= check_box_tag "cities", "#{city}", (@cities.include? city) ? true : false, :id => "cities_#{city}" %>
    <% end %>
    <%= submit_tag 'Filter', id: "cities_submit" %>
<% end %>

<table>
  <tr>
    <th>Good</th>
    <th>Price</th>
    <th>Date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sales.each do |sale| %>
  <tr>
    <td><%= sale.good %></td>
    <td><%= sale.price %></td>
    <td><%= sale.date %></td>
    <td><%= link_to 'Show', sale %></td>
    <td><%= link_to 'Edit', edit_sale_path(sale) %></td>
    <td><%= link_to 'Destroy', sale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sale', new_sale_path %>
